[project]
name = "persona-chat"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "flask>=3.0.0",
]

[tool.vercel]
entrypoint = "api.chat:app"
